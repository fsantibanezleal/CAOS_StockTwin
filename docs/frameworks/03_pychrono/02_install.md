# PyChrono, install

```bash
conda install -c projectchrono pychrono   # see environment-dem.yml
```

Lane: **offline, calibration only**. Pinned in the matching requirements file, so a clone reproduces the exact version this product was built against.
