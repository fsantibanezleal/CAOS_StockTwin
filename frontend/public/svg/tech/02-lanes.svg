<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 880 468" width="880" class="arch-svg" role="img">
  <style>
    .bx  { fill: var(--color-surface-2, var(--color-surface)); stroke: var(--color-border); stroke-width: 1; }
    .bx-hi   { fill: var(--color-surface-2, var(--color-surface)); stroke: var(--color-accent); stroke-width: 1.6; }
    .bx-web  { fill: var(--color-surface-2, var(--color-surface)); stroke: var(--color-good, #3fb950); stroke-width: 1.4; }
    .bx-off  { fill: var(--color-surface-2, var(--color-surface)); stroke: var(--color-warn, #d29922); stroke-width: 1.4; }
    .bx-gate { fill: none; stroke: var(--color-warn, #d29922); stroke-width: 1.2; stroke-dasharray: 5 3; }
    .ttl { font: 600 12.5px system-ui, sans-serif; fill: var(--color-fg); }
    .sub { font: 10px system-ui, sans-serif; fill: var(--color-fg-faint); }
    .it  { font: 10.5px system-ui, sans-serif; fill: var(--color-fg-subtle); }
    .mono { font: 10px ui-monospace, monospace; fill: var(--color-accent); }
    .mu  { font: 10px system-ui, sans-serif; fill: var(--color-fg-faint); font-style: italic; }
    .lbl { font: 9.5px system-ui, sans-serif; fill: var(--color-fg-faint); }
    .hdr { font: 700 11px system-ui, sans-serif; fill: var(--color-fg-faint); letter-spacing: 0.06em; }
    .flow { fill: none; stroke: var(--color-fg-faint); stroke-width: 1.5; marker-end: url(#a); }
    .flow-hi { fill: none; stroke: var(--color-accent); stroke-width: 1.8; marker-end: url(#ah); }
  </style>
  <defs>
    <marker id="a" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="var(--color-fg-faint)"/>
    </marker>
    <marker id="ah" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="var(--color-accent)"/>
    </marker>
  </defs>
  <text class="hdr" x="14" y="20">LIVE, IN THE BROWSER</text>
  <rect class="bx-web" x="14" y="28" width="852" height="106" rx="6"/>
  <text class="ttl" x="28" y="48">TypeScript engine, recomputed on every control change</text>
  <text class="mono" x="28" y="63">frontend/src/engine/{heightfield,segregation,pile,stacking,blending,rtd,stream,run}.ts</text>
  <text class="it" x="28" y="82">- the priority-cascade relaxation, the five stacking paths, the four reclaim geometries, the lot ledger</text>
  <text class="it" x="28" y="97">- Gray-Thornton segregation, the variance reduction ratio, the variograms, the residence-time distribution</text>
  <text class="it" x="28" y="112">- budget: 100 ms slider to redraw, 8 ms per relaxation sweep. Measured, and reported on the page.</text>
  <text class="mu" x="28" y="128">Not Pyodide: a cold start plus per-frame marshalling cannot meet the 100 ms budget.</text>
  <text class="hdr" x="14" y="158">OFFLINE, PRECOMPUTE</text>
  <rect class="bx-off" x="14" y="166" width="418" height="132" rx="6"/>
  <text class="ttl" x="28" y="186">The canonical bake, an explicit release operation</text>
  <text class="mono" x="28" y="201">python data-pipeline/run.py</text>
  <text class="it" x="28" y="220">- 31-seed credible bands per case</text>
  <text class="it" x="28" y="235">- oreblocks ore bodies, GSTools simulation</text>
  <text class="it" x="28" y="250">- surrogate training and ONNX export</text>
  <text class="it" x="28" y="265">- the complete case by metric matrix</text>
  <text class="mu" x="28" y="288">Tests write only to a sandbox output root.</text>
  <rect class="bx-off" x="448" y="166" width="418" height="132" rx="6"/>
  <text class="ttl" x="462" y="186">Discrete element, a separate conda environment</text>
  <text class="mono" x="462" y="201">environment-dem.yml  ·  stlab/stages/calibrate.py</text>
  <text class="it" x="462" y="220">- a bidisperse pour calibrates the segregation number</text>
  <text class="it" x="462" y="235">- PyChrono has no pip wheel, so the lane is separate</text>
  <text class="it" x="462" y="250">- kill criterion: if it cannot run, method 7 is DELISTED</text>
  <text class="it" x="462" y="265">- and the calibration falls back to published distances</text>
  <text class="mu" x="462" y="288">The Benchmark page states which path was taken.</text>
  <text class="hdr" x="14" y="324">REPLAY</text>
  <rect class="bx" x="14" y="332" width="852" height="68" rx="6"/>
  <text class="ttl" x="28" y="352">The committed compact trace and its manifest</text>
  <text class="mono" x="28" y="367">data/derived/&lt;case&gt;/trace.json  ·  data/derived/manifests/&lt;case&gt;.json</text>
  <text class="it" x="28" y="386">First paint, and the cross-case pages. The trace carries EVENTS only: every verdict is recomputed in the browser.</text>
  <rect class="bx-gate" x="14" y="412" width="852" height="42" rx="6"/>
  <text class="ttl" x="28" y="430">THE GATE decides the lane by MEASUREMENT, not by intent</text>
  <text class="mono" x="28" y="446">stlab/core/gate.py::classify_lane()  -&gt;  verdict and budgets into the manifest; CI fails on a mislabelled lane</text>
</svg>
